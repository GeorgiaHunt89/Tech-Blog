<!DOCTYPE html>
<html lang="en">

<head>
    <title>Tech Blog</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://unpkg.com/tailwindcss@^2/dist/tailwind.min.css" rel="stylesheet">
    <script src="https://use.fontawesome.com/releases/v5.15.3/js/all.js" data-auto-replace-svg="nest"></script>
    <link href="../../public/style.css" rel="stylesheet">
</head>
<body>
    <header>
        <h1>
        <p class="blog-title"> Tech Blog <i class="fas fa-laptop-code"></i></p>
      </h1>
        <nav>
            <ul>
                <li><a href="/">Home</a></li>
                {{#if loggedIn}}<li><a href="/dashboard">Dashboard</a></li>{{/if}}
                {{#if loggedIn}}<li><a href="/homepage">Homepage</a></li>{{/if}}
                {{#unless loggedIn}}<li><a href="/dashboard">Login</a> / <a href="/Dashboard">Signup</a></li>{{/unless}}
                {{#if loggedIn}}<li><a href="/logout" id="logout">Logout</a></li>{{/if}}
            </ul>
        </nav>
    </header>
    {{{body}}}
</body>
</html>